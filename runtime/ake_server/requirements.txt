# ake_server's own dependencies — kept separate from the root requirements.txt (untouched;
# still just openpyxl>=3.0 for ake/ itself). Install both: -r requirements.txt -r ake_server/requirements.txt
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
pydantic>=2.0
